@model IEnumerable<ProjetoBackend.Models.Servico>

@{
    ViewData["Title"] = "Index";
}
<div class="container">
    <div style="font-family:'Shrikhand', sans-serif;">
        <h1 class="text-light">Serviços</h1>
    </div>
    <div style="font-family:'Shrikhand', sans-serif;">
        <p>
            <a asp-action="Create" class="text-black text-decoration-none text-decoration-underline">Cadastrar</a>
        </p>
    </div>
    <div style="font-family:'Itim',sans-serif;">
        <table class="table">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Nome)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.ValorServico)
                    </th>
                    <th></th>
                </tr>
            </thead>
            <body>
                @foreach (var item in Model)
                {
                <tr>
                    <td>
                            @Html.DisplayFor(modelItem => item.Nome)
                    </td>
                    <td>
                            @Html.DisplayFor(modelItem => item.ValorServico)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.ServicoId">Edit</a> |
                        <a asp-action="Details" asp-route-id="@item.ServicoId">Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.ServicoId">Delete</a>
                    </td>
                </tr>
                }
            </body>
    </div>
    </table>
</div>